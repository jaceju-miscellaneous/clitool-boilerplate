{
    "name": "vendor-name/app-name",
    "description": "The command line tool.",
    "minimum-stability": "dev",
    "license": "MIT",
    "authors": [
        {
            "name": "Your Name",
            "email": "your@email.com"
        }
    ],
    "require": {
        "php": ">=5.4.0",
        "corneltek/cliframework": ">=2.2.0"
    },
    "require-dev": {
        "kherge/box": "2.5.2"
    },
    "autoload": {
        "classmap": [
            "src/",
            "tests/"
        ]
    },
    "scripts": {
        "build": "./vendor/bin/box build && mv bin/app.phar bin/app"
    },
    "bin": [
        "bin/app"
    ]
}
